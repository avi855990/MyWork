# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['shooter.py'],
    pathex=[],
    binaries=[],
    datas=[('background.mp3', '.'), ('icon.png', '.')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='Busting in Space',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon=['app-icon.icns'],
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='Busting in Space',
)
app = BUNDLE(
    coll,
    name='Busting in Space.app',
    icon='app-icon.icns',
    bundle_identifier=None,
)
